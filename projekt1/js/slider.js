const swiper = new Swiper('.swiper', {

    loop: true,
    centeredSlides: true,
    effect: 'coverflow',
 autoplay: {
        delay: 2500,
        disableOnInteraction: false,
      },

});
